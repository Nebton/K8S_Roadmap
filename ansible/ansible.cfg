[defaults]
# Configurations for file handling
stdout_callback = yaml  # Use YAML format for stdout output

# Enable debugging output
verbosity = 3  

# Set the location of the configuration file to the current directory
config_file = ./ansible.cfg  

# Optional: Enable any additional debugging flags
log_path = ./ansible-debug.log  # Save logs to a file

